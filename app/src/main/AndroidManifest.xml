<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.whx.animpractice">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity"
            android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AppbarLayoutTestActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".behavior.BottomBehaviorActivity"/>

        <activity android:name=".behavior.SwipeBehaviorActivity" />

        <activity android:name=".property.ValueAnimatorTestActivity"/>

        <activity android:name=".property.ObjectAnimatorTestActivity"/>

        <activity android:name=".transition.TransitionActivityA"/>

        <activity android:name=".transition.TransitionActivityB"/>

        <activity android:name=".transition.change_activity.MakeSceneTransitionActivity"/>

        <activity android:name=".transition.share_element.RecyclerActivity"/>

        <activity android:name=".transition.share_element.PreviewActivity"/>

        <activity android:name=".transition.scene.SceneMainActivity"/>

        <activity android:name=".transition.scene.AutoTransitionActivity"/>

        <activity android:name=".transition.scene.ChangeTransformActivity"/>

        <activity android:name=".transition.scene.ChangeClipBoundsActivity"/>

        <activity android:name=".transition.scene.ChangeImageTransformActivity"/>

        <activity android:name=".transition.scene.VisibleTransitionActivity"/>

        <activity android:name=".transition.scene.TransitionSetActivity"/>

        <activity android:name=".transition.change_activity.SwitchAnimActivity"/>

        <activity android:name=".transition.change_activity.SwitchAnimTargetActivity"
            android:theme="@style/AppTheme.Slide"/>

        <activity android:name=".transition.share_element.SimpleShareElementActivity"/>

        <activity android:name=".transition.share_element.SimpleAnotherActivity"/>

    </application>

</manifest>